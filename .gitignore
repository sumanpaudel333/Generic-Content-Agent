# Business config -- your real details, never committed
config/config.yaml

# Secrets
.env

# Fetched mail-relay certificate -- environment-specific, refetch with
# python -m scripts.fetch_smtp_cert
config/smtp_relay_cert.pem

# Python
venv/
__pycache__/
*.pyc
*.pyo

# Local runtime data
logs/*.db
logs/*.log
logs/*.jsonl
data/
training_data/

# Trained model artifacts (large binaries -- distribute separately)
*.gguf
